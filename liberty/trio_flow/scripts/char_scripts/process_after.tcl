# place holder for updating cell level libs on client during assembly stage as soon as cell level libs are generated
if {[distr_on_client_assembly]} {
   puts "INFO (trio-flow): processing $::env(libraArcCharDir) "
}

